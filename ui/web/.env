# The browser calls the API on the host-published port, not the compose service name
# ("gold-api") — this JS runs in the user's browser, outside the compose network.
VITE_API_BASE_URL=http://localhost:8000
