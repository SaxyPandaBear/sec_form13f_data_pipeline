# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Airflow runtime state (mounted into containers, not source)
airflow-logs/
logs/
*.log
standalone_admin_password.txt

# Docker Compose named volumes if ever bind-mounted locally
postgres-db-volume/
localstack-data/
trino-data/

# LocalStack local state
.localstack/

# Environment / secrets
.env
.env.*
# Not a secret: the UI's build-time default API URL, meant to be checked in
# (see ui/web/.env's own comment).
!ui/web/.env

# Node (ui/web)
node_modules/
dist/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code local settings
.claude/settings.local.json
